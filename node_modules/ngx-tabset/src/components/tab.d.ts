import { TemplateRef } from "@angular/core";
import { TabHeadingDirective } from "./tab-heading";
export declare class TabComponent {
    heading: TabHeadingDirective;
    title: string;
    active: boolean;
    disabled: boolean;
    readonly headingTemplate: TemplateRef<any> | null;
}
